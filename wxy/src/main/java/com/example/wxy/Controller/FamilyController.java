package com.example.wxy.Controller;

import com.example.wxy.entity.Family;
import org.springframework.web.bind.annotation.*;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;
import java.util.Collections;
import java.util.List;
@RestController
public class FamilyController {
   // @Autowired
    //private FamilyMapper familyMapper;
//@GetMapping("/Family")
//public List query() {
   // List<Family>list = familyMapper.find();
    //System.out.println(list);
    //return list;
//}
//@PostMapping("/Family")
    //public List save(Family family) {
    //int i = familyMapper.insert(family);
    //if(i > 0){
       //return Collections.singletonList("插入成功");
    //}else {
        //return Collections.singletonList("插入失败");
    //}
//}
}
